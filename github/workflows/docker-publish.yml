name: Build and Push Docker Image

on:
  push:
    branches: [ "main" ]  # Jalan tiap push ke main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login ke Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: .  # folder root, tempat ada Dockerfile
          push: true
          tags: ramahaxor/ramadevops-django:latest, ramahaxor/ramadevops-django:${{ github.sha }}
